apiVersion: v1
kind: Service
metadata:
  name: landslide-service
spec:
  type: LoadBalancer
  selector:
    app: landslide   # ← change this
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000